import helpers from '../helpers';

const {  } = helpers.ingredients;

const updateIngredientFood = async (ingredientID, content) => {

}

const deleteIngredientFood = async (ingredientID) => {
    
}

export { updateIngredientFood, deleteIngredientFood }